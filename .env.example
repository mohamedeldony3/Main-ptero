# .env.example — نسخة للتعديل. لا تقم بعمل commit للملف النهائي الذي يحتوي بيانات حساسة.
# على Render ضَع هذه القيم في Environment -> Environment Variables

# URL كامل (يمكن أن يتضمن المسار إذا كان السكربت ليس في الجذر)
URL=https://ptero2.melsony.site/path/to/script.sh

# Host (نفس المضيف المستخدم في .netrc وطلب HTTPS)
HOST=ptero2.melsony.site

# اسم المستخدم وكلمة المرور
USER=admin
PASS=12345